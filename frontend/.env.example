# Frontend environment variables (copy to .env for local development)
# See docs/technology/03-environment-strategy.md

VITE_APP_ENV=local
VITE_API_BASE_URL=/api/v1

# Used by quasar.config.ts devServer proxy (not exposed to client)
BACKEND_URL=http://localhost:8080
