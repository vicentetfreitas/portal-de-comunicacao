/**
 * FT-DOCUMENTO API paths — relative to HTTP client base URL (/api/v1).
 */
export const PASTA_API_PATHS = {
  base: "/pastas"
} as const;

export const DOCUMENTO_API_PATHS = {
  base: "/documentos"
} as const;
