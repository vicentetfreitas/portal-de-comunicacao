spring:
  config:
    activate:
      on-profile: hml

  jpa:
    show-sql: false

application:
  security:
    cors-allowed-origins:
      - https://hml.portal.unimedceara.com.br
  persistence:
    pool-max-size: 20
